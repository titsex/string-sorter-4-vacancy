import { join } from 'path'

export const PUBLIC_FOLDER_PATH = join(__dirname, '..', 'public')

export const INPUT_FILE_PATH = join(PUBLIC_FOLDER_PATH, 'input.txt')

export const OUTPUT_FILE_PATH = join(PUBLIC_FOLDER_PATH, 'output.txt')

export const CHUNK_FOLDER_PATH = join(PUBLIC_FOLDER_PATH, 'chunks')
